# Marc Reantaso — Personal Portfolio (React + Tailwind)

## Quick start

1. Install dependencies:
   ```bash
   npm install
   ```
2. Start development server:
   ```bash
   npm start
   ```
3. Open http://localhost:3000 in your browser

## Notes

- This project uses Tailwind CSS. If you want to customize colors or theme, edit `tailwind.config.js`.
- Replace project descriptions, links, and the email in `src/components/Contact.js` with your real info.
